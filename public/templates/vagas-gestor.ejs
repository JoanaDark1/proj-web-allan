<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Minhas Vagas - Gestor | MedOportuna</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #ffffff; font-family: "Poppins", sans-serif; }
        .navbar { background-color: #2b7e4c !important; }
        header { background-color: #191970; color: white; }
        .card-title { font-weight: 600; }
        footer { background-color: #2b7e4c; }
        footer p { color: #ffffff; margin-bottom: 0.25rem; }
        footer a { color: white; }
        .btn-success { font-size: 1.1rem; }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark px-4">
        <a class="navbar-brand" href="/">MedOportuna</a>
        <a href="/templates/auth/login.html" class="btn btn-outline-light">Sair</a>
    </nav>

    <header class="py-5 text-white text-center">
        <div class="container">
            <h1>Minhas Vagas Publicadas</h1>
            <p class="lead">Gerencie suas vagas ativas na plataforma</p>
        </div>
    </header>

    <main class="container my-5">
        <div class="row">
            <% if (vagas.length === 0) { %>
                <div class="col-12 text-center">
                    <p class="text-muted">Você ainda não publicou nenhuma vaga.</p>
                </div>
            <% } else { %>
                <% vagas.forEach(vaga => { %>
                    <div class="col-md-6 mb-4">
                        <div class="card shadow-sm">
                            <div class="card-body">
                                <h5 class="card-title"><%= vaga.titulo %></h5>
                                <p class="card-text"><%= vaga.local %></p>
                                <p class="card-text"><%= vaga.remuneracao %></p>
                                <p class="card-text"><%= vaga.descricao %></p>
                                <p class="card-text">
                                    Publicada em: 
                                    <%= new Date(vaga.data_publicacao).toLocaleDateString('pt-BR') %>
                                </p>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </main>

    <footer class="text-center py-4">
        <p class="mb-1">© 2025 MedOportuna. Todos os direitos reservados.</p>
        <p class="small">Contato: <a href="mailto:contato@medoportuna.com">contato@medoportuna.com</a></p>
    </footer>
</body>
</html>
