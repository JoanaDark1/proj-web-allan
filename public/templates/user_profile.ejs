<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página do Usuário - <%= nome %></title>
</head>
<body>
    <h1><%= nome %></h1>
    <img src="<%= foto_perfil %>" alt="Foto de perfil">

    <% if (tipo === 'medico') { %>
        <p>Conteúdo específico para Médicos.</p>
    <% } else if (tipo === 'enfermeiro') { %>
        <p>Conteúdo específico para Enfermeiros.</p>
    <% } %>

    <a href="/">Sair</a>
</body>
</html>
